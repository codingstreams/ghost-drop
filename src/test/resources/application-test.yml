spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
  flyway:
    enabled: true

file:
  upload-dir: test-uploads/

server:
  address: 0.0.0.0
  port: 5767

app:
  base-url: http://fruitloop.local